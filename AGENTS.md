# J2 프로젝트 지침

작업 전에 루트의 [개발 규칙.md](개발%20규칙.md)를 읽고 현재 코드 수정과 이후 모든 개발에 적용한다.
가독성 규칙: 함수 사이 빈 줄, 함수 내부 로직 단위 빈 줄, 조건문 및 반복문 본문 중괄호 필수. 변수 선언·패킷 생성·패킷 전송은 구분한다.
사용자가 수정 중인 코드와 에셋을 보존하고, 서식 정리로 게임 동작을 변경하지 않는다.
멤버 변수는 사용자 정의 타입·기본 타입·Unity/UI 타입별로 모으고 그룹 사이 빈 줄을 둔다. 타입과 이름 사이는 실제 탭(너비 4)으로 정렬한다. 초기화 의존 순서는 보존한다.
직접 작성하는 C#에는 using System; using System.Collections; using System.Collections.Generic;을 기본 선언한다. 컬렉션 타입은 짧은 이름을 쓰고 Unity와 이름 충돌 시 명시적으로 구분한다.
