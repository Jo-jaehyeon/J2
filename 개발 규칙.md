# J2 개발 규칙

이 규칙은 현재 개발된 코드와 앞으로 작성·수정하는 프로젝트 코드에 적용한다.

## 가독성

- 함수와 함수 사이에는 빈 줄을 한 줄 이상 둔다.
- 함수 내부도 코드를 한 줄에 몰아 쓰지 않는다. 실행문은 각각 별도 줄에 작성한다.
- 같은 함수라도 담당하는 로직이 바뀌면 빈 줄로 구분한다.
- 변수 선언, 조건 검사, 패킷 생성, 패킷 발송은 서로 구분되는 로직이다. 예를 들어 패킷 생성과 SendPacket 호출 사이에는 빈 줄을 둔다.
- if, else와 같이 조건에 따라 실행되는 본문은 한 줄이어도 반드시 `{ }`로 감싼다.
- for, foreach, while, do 및 using, lock 등의 본문에도 중괄호를 사용한다. `else if` 연결은 유지할 수 있다.
- 들여쓰기는 공백 4칸, 중괄호는 다음 줄에 배치한다.
- 서식을 정리할 때 기존 실행 순서와 동작은 변경하지 않는다.

```csharp
void RequestMatch()
{
    var network = NetworkManager.Instance;

    if (network == null || !network.IsConnected)
    {
        return;
    }

    var packet = new C_FindMatch
    {
        SessionId = network.SessionId,
        MatchType = 0,
        MMR = 0
    };

    network.SendPacket(PacketId.CFindmatch, packet);
}

void CloseMenu()
{
    mainMenu = false;
}
```

## 적용과 확인

- 작업 전에 이 문서를 읽고 새 코드뿐 아니라 수정하는 기존 코드에도 적용한다.
- 의미상 로직 구분은 사람이 검토한다. 자동 서식만으로 검토를 대신하지 않는다.
- `Tools/CodeStyle`의 문법 기반 도구로 프로젝트 C#의 중괄호와 기본 서식을 정리할 수 있다.
- protobuf 등 자동 생성 파일과 외부 라이브러리는 직접 재서식하지 않는다. 필요한 경우 생성기에서 적용한다. 직접 관리하는 PacketCodec과 핸들러 본문은 적용 대상이다.

## 클래스 멤버 변수

- 직접 만든 클래스·자료형, bool/int/string 등의 기본 타입, Unity/UI 타입끼리 모아 선언한다. 컬렉션·배열은 담는 타입을 기준으로 분류한다.
- 서로 다른 카테고리 사이에는 빈 줄을 한 줄 둔다.
- 타입 뒤에서 변수 이름까지 실제 탭 문자를 사용하여 같은 그룹의 변수 이름이 같은 열에서 시작하도록 맞춘다. 탭 너비는 4칸으로 본다.
- 줄 앞 들여쓰기는 기존대로 공백 4칸을 사용한다. 이름 정렬용 탭과 들여쓰기를 구분한다.
- 초기화식이 다른 멤버를 참조하거나 실행 순서에 영향을 주는 경우 초기화 순서를 보존하고, 해당 선언을 별도 그룹으로 둔다. 전처리 구간이나 주석·직렬화 특성을 잃지 않는다.

## 기본 using

- 직접 작성하는 C# 파일에는 다음 세 네임스페이스를 기본으로 선언한다.

```csharp
using System;
using System.Collections;
using System.Collections.Generic;
```

- Dictionary뿐 아니라 List, HashSet, Queue 등은 `System.Collections.Generic.`을 반복하지 않고 짧은 타입 이름으로 쓴다.
- Object, Random 등 Unity 타입과 이름이 충돌하는 경우에는 명시적 네임스페이스 또는 using 별칭으로 구분한다.
- 자동 생성 파일은 기존 제외 원칙을 유지한다.

## 객체 지향과 모듈화

- 프로젝트 전반에서 공통 상태·동작·처리 흐름 등 공통 패턴이 있는 클래스는 상위 부모 클래스로 공통 부분을 추출하고, 이를 상속받아 역할에 맞게 세분화한다. 플레이어와 기물은 이 원칙의 예시이며 적용 대상을 한정하지 않는다.
- 부모 클래스에는 공통 책임을 두고, 플레이어 전용 조작이나 기물 전용 전투 판단 등 개별 책임은 해당 자식 클래스 또는 별도 컴포넌트에 둔다.
- 하나의 클래스에 연관 없는 기능을 모아 넣지 않는다. 서로 관련 있는 기능을 하나의 책임 단위로 묶어 별도 클래스로 만들고 활용한다.
- 예를 들어 플레이어 입력과 조작은 `PlayerController`에서 담당하고, 패킷 송수신·스폰 데이터 조회·UI 표시는 각각 해당 책임을 가진 클래스에서 담당한다.
- 상속은 공통 객체의 상태와 동작을 확장하는 데 사용한다. 서로 다른 객체가 일부 기능만 공유하는 경우에는 별도 컴포넌트나 인터페이스로 재사용하여 불필요한 상속 의존성을 만들지 않는다.
- 새 코드를 작성하거나 기존 기능을 수정할 때 이 원칙을 적용한다. 기존 구조를 분리할 때는 외부 호출 계약, Unity 직렬화 참조와 기존 동작을 보존하고 변경한 기능을 검증한다.
